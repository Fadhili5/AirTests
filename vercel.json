{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm --workspace @lending/shared run build && npm --workspace @lending/web run build",
  "outputDirectory": "apps/web/.next",
  "env": {
    "NEXT_PUBLIC_API_BASE_URL": "@NEXT_PUBLIC_API_BASE_URL"
  }
}
